# Sable (Photoshop UXP)

This folder is the plugin Photoshop loads — open **`manifest.json`** in UXP Developer Tools.

Files: `manifest.json`, `index.html`, `index.js`, and `images/` for panel icons.
